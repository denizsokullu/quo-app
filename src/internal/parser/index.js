import { AbstractComponent } from './abstractComponent';
//understand what an editable object needs
//start with preview
//
//preview of a page =>
export default function sketchParser(data){
  return new AbstractComponent(data);
}
